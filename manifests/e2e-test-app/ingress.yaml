apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: e2e-test-app
  namespace: applications
spec:
  rules:
  - host: e2e-test-app.tykus.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: e2e-test-app
            port:
              number: 80
